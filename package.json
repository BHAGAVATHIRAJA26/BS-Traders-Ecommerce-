{
  "name": "bs-traders-ecommerce-root",
  "version": "1.0.0",
  "description": "Root package for e-commerce project deployment",
  "main": "backend/login.js",
  "scripts": {
    "start": "node backend/login.js",
    "build": "cd backend && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18"
  }
}
